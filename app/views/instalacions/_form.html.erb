<%= form_for(@instalacion) do |f| %>
  <% if @instalacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instalacion.errors.count, "error") %> prohibited this instalacion from being saved:</h2>

      <ul>
      <% @instalacion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :recinto_id %><br />
    <%= f.number_field :recinto_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
